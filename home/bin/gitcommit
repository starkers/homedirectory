#!/usr/bin/env bash

if [ ! X"$1" == X ]; then
  git commit -m "$@"
  exit 0
fi

echo "##################################################################################"
read -p "msg: " FOO

if [ ! "X${FOO}" == X ]; then
  git commit -m "${FOO}"
fi
