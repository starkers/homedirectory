#!/usr/bin/env bash

if [ ! X"$1" == X ]; then
  git commit -m "${FOO}"
  exit 0
fi

set -eu
echo "##################################################################################"
read -p "msg: " FOO

if [ ! X${FOO} == X ]; then
  git commit -m "${FOO}"
fi
